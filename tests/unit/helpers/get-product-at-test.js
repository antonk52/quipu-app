
import { getProductAt } from 'quipu-app/helpers/get-product-at';
import { module, test } from 'qunit';

module('Unit | Helper | get product at');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getProductAt([42]);
  assert.ok(result);
});

