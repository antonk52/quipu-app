
import { totalSpentByUser } from 'quipu-app/helpers/total-spent-by-user';
import { module, test } from 'qunit';

module('Unit | Helper | total spent by user');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = totalSpentByUser([42]);
  assert.ok(result);
});

