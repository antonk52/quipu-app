
import { outputEmail } from 'quipu-app/helpers/output-email';
import { module, test } from 'qunit';

module('Unit | Helper | output email');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = outputEmail([42]);
  assert.ok(result);
});

