
import { outputProductByIndex } from 'quipu-app/helpers/output-product-by-index';
import { module, test } from 'qunit';

module('Unit | Helper | output product by index');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = outputProductByIndex([42]);
  assert.ok(result);
});

