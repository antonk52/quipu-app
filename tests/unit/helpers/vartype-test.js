
import { vartype } from 'quipu-app/helpers/vartype';
import { module, test } from 'qunit';

module('Unit | Helper | vartype');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = vartype([42]);
  assert.ok(result);
});

