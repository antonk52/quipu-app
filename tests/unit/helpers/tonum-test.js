
import { tonum } from 'quipu-app/helpers/tonum';
import { module, test } from 'qunit';

module('Unit | Helper | tonum');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tonum([42]);
  assert.ok(result);
});

